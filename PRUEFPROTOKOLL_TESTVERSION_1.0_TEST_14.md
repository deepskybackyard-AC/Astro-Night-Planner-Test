# Prüfprotokoll 1.0.0-test.14

Geprüft:

- JavaScript-Syntax von `assets/app.js`
- CSS-Änderungen in `assets/styles.css`
- Versionsangaben in `assets/build-config.js`, `VERSION.json` und `sw.js`
- ZIP-Integrität

Nach dem Upload auf GitHub Pages zusätzlich im Browser prüfen:

- neues Filterlayout Desktop und Smartphone
- Objektauswahl mit verschiedenen Filterwerten
- Service-Worker-Aktualisierung
